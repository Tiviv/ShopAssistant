-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
# A local fake SMTP server for testing password-reset emails without real
# credentials or actually sending mail — see tests/test_password_reset.py.
aiosmtpd==1.4.6
